import { useState } from "react";
import { Link, useNavigate } from "react-router-dom";
import { Building2, CheckCircle2, Eye, EyeOff, Lock, Mail, User } from "lucide-react";
import { useAuth } from "../context/AuthContext.jsx";

export default function TenantRegister() {
  const { registerTenant } = useAuth(); const navigate = useNavigate();
  const [form, setForm] = useState({ name: "", email: "", password: "", confirm: "" }); const [show, setShow] = useState(false); const [error, setError] = useState("");
  const set = (key) => (event) => setForm({ ...form, [key]: event.target.value });
  const submit = (event) => { event.preventDefault(); setError(""); if (form.password !== form.confirm) return setError("Passwords do not match."); const result = registerTenant(form); if (result.ok) navigate("/tenant", { replace: true }); else setError(result.error); };
  return <div className="min-h-screen grid lg:grid-cols-2 bg-stone-50 dark:bg-stone-950"><section className="hidden lg:flex flex-col justify-between bg-stone-950 text-white p-12 relative overflow-hidden"><div className="absolute inset-0 bg-[radial-gradient(circle_at_top_right,_#0284c7_0,_transparent_42%)] opacity-60" /><div className="relative flex items-center gap-3"><div className="h-11 w-11 rounded-2xl bg-sky-500 grid place-items-center"><Building2 size={22} /></div><div><p className="font-bold">EstateHub</p><p className="text-xs text-sky-200">Resident portal</p></div></div><div className="relative max-w-md"><p className="text-sky-300 text-sm font-semibold">A home that works for you</p><h1 className="text-4xl font-bold leading-tight mt-3">Everything you need as a resident, in one calm place.</h1><p className="text-stone-300 mt-5 leading-relaxed">Pay rent, track maintenance, review your lease, and contact your property team when you need them.</p></div><p className="relative text-xs text-stone-500">EstateHub · Resident portal</p></section><main className="flex items-center justify-center p-6 sm:p-10"><div className="w-full max-w-md"><Link className="lg:hidden flex items-center gap-2 mb-8 font-bold" to="/login"><Building2 className="text-sky-600" />EstateHub</Link><p className="text-sm font-semibold text-sky-600">RESIDENT ACCESS</p><h2 className="text-2xl font-bold mt-2">Activate your tenant account</h2><p className="text-sm text-stone-500 mt-2">Use the same email your property manager has on file.</p><form className="space-y-4 mt-7" onSubmit={submit}>{error && <div className="p-3 rounded-xl border border-rose-200 bg-rose-50 text-rose-700 text-sm">{error}</div>}<div><label className="label">Full name</label><div className="relative"><User className="absolute left-3 top-1/2 -translate-y-1/2 text-stone-400" size={16} /><input className="input pl-9" value={form.name} onChange={set("name")} required /></div></div><div><label className="label">Email address</label><div className="relative"><Mail className="absolute left-3 top-1/2 -translate-y-1/2 text-stone-400" size={16} /><input type="email" className="input pl-9" value={form.email} onChange={set("email")} required /></div></div><div><label className="label">Create password</label><div className="relative"><Lock className="absolute left-3 top-1/2 -translate-y-1/2 text-stone-400" size={16} /><input type={show ? "text" : "password"} className="input pl-9 pr-10" value={form.password} onChange={set("password")} minLength="6" required /><button type="button" className="absolute right-3 top-1/2 -translate-y-1/2 text-stone-400" onClick={() => setShow(!show)}>{show ? <EyeOff size={16} /> : <Eye size={16} />}</button></div></div><div><label className="label">Confirm password</label><input type={show ? "text" : "password"} className="input" value={form.confirm} onChange={set("confirm")} minLength="6" required /></div><button className="btn-primary bg-sky-600 hover:bg-sky-700 w-full justify-center mt-2"><CheckCircle2 size={16} />Activate resident access</button></form><p className="text-center text-sm text-stone-500 mt-6">Already activated? <Link className="font-semibold text-sky-700" to="/login">Sign in</Link></p><p className="mt-8 rounded-xl bg-stone-100 p-4 text-xs text-stone-500">Need access? Your property manager must first add your tenant profile using this email.</p></div></main></div>;
}
